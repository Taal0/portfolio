export function PageLoader() {
  return (
    <div className="flex flex-1 items-center justify-center py-20 text-sm text-muted">
      <span className="animate-pulse text-accent">▊ loading...</span>
    </div>
  )
}
